# OPL Book Forge Status

Owner: `opl-bookforge`
Purpose: `current_status`
State: `active_truth`
Machine boundary: Human-readable status summary. Machine truth remains in contracts, agent pack files, OPL validator output, runtime receipts, evidence receipts, owner receipts, and typed blockers.

`contracts/standard_agent_conformance_profile.json` 由 Book Forge 自己声明五阶段 golden path 与物理 surface 分类；OPL conformance engine 只执行该 profile，不再内置书籍领域 stage、surface 或 residue 常量。该结构校验不构成书稿质量、出版通过或 production-ready 证据。

Current state: OPL standard structural baseline plus one historical real short-book pilot evidence package with owner-gated blockers that prevent production-ready, owner-accepted, publication-approved, final-export-ready, or hosted-runtime-ready claims.

Book Forge follows the family rule that functional / structural cleanup can land before later evidence lanes. The canonical policy lives in [the active gap plan](./active/bookforge-ideal-state-gap-plan.md) and [invariants](./invariants.md): structural validation, OMA evidence, pilot exports, rendered pages, docs, or helper proof plumbing cannot replace owner acceptance, final export acceptance, publication-proof visual acceptance, runtime / hosted parity, physical delete authorization, real long-book evidence, or production-ready evidence.

## Current Validated Surfaces

- Default `scripts/verify.sh` is the fast policy lane. `structural` adds OPL scaffold/interface/source-hygiene readback, `helpers` adds native-helper probes and adapter tests, `pdf` runs two real review/proof compile-render E2E cases, `full-local` executes the repo-owned policy/helper/PDF/handler union, and `full` adds the OPL structural readback. `full-local` does not replace framework conformance when the canonical OPL parser is current. Negative artifact-gate variants use a table-driven call to the canonical gate instead of recompiling identical input.
- `package.json`, the Codex plugin manifest, and `contracts/opl_agent_package_manifest.json` now agree on repo-local package version `0.3.4`; the owner manifest declares canonical `agent_id/package_id=obf`, matching `contracts/pack_compiler_input.json.canonical_agent_id`. The bundled proof profile explicitly uses CTEX-distributed Fandol fonts and declares their exact TeX dependencies, so real PDF smoke no longer relies on macOS font auto-detection or user-only fonts. The PDF native-helper probe binds its publication command and artifact-write slots to the exact helper bytes, without granting route, lifecycle, executor, receipt, verdict, or readiness authority. Published refs, digests, and channel receipts remain OPL release-authority outputs; `opl-bookforge` remains only the repo/domain/Foundry/npm/plugin/distribution carrier locator and is not retained as a package alias.
- `agent/primary_skill/SKILL.md` is the repo-owned rich primary skill source and the default Codex entry for OPL Book Forge. `plugins/opl-bookforge/skills/opl-bookforge/SKILL.md` is the materialized Codex carrier full copy used by plugin installation; the carrier remains a transport surface, not a membership/status axis, and does not become book-domain truth, domain readiness evidence, manuscript quality evidence, publication approval, final-export acceptance, or owner acceptance.
- `contracts/capability_map.json` exposes Agent Lab self-evolution routing metadata for each standard capability, including the default primary skill and the canonical repo-local workflow professional skills under `agent/professional_skills/*/SKILL.md`: improvement tokens, canonical target paths, verification refs, forbidden surfaces, exposure policy, and owner closeout boundary. The map is a resolver index only; it cannot write owner receipts, create typed blockers, write memory bodies, or claim domain/production readiness.
- `contracts/foundry_agent_series.json` is the Book Forge consumer pin for the canonical OPL Foundry policy export, canonical policy contracts, and shared policy release; it retains Book Forge identity and only false consumer-authority flags, rather than copying membership, projection, lifecycle, or authority policy bodies. Together with `contracts/generated_surface_handoff.json`, it keeps Book Forge as a `standard_domain_agent`, leaves generated CLI/MCP/skill/product-entry surfaces OPL-owned, and does not make generated surfaces a membership/status axis.
- `contracts/temporal_stage_run_consumption_policy.json` declares Book Forge as a refs-only consumer of OPL Temporal-backed StageRun/provider attempt refs. OPL owns Temporal runtime, StageRun, provider attempts, queues, generated status/workbench surfaces, and the attempt ledger; Book Forge owns book truth, manuscript quality, export/publication verdict refs, memory body, owner receipt body, and minimal book-native authority functions. The policy also names the allowed Book Forge export surface for OPL generated surfaces: action contracts, chapter task cards, manuscript/style/artifact authority refs, owner-gated publication/export decision refs, typed blocker refs, and owner receipt refs. Private Temporal/StageRun wrappers, direct `opl-bookforge` runtime CLI as the default entry, schedulers, queues, session stores, provider completion stores, attempt ledgers, runtime status read models, and evidence-package bypasses are explicitly forbidden domain exports/default entries. Its completion audit separates OPL provider/generated/StageRun accounts from Book Forge `review_pdf`, `publication_proof`, `final_export`, and owner-acceptance accounts; provider completion, generated surface readiness, and StageRun status readiness cannot close any of those Book Forge accounts. Revision, publication/final export, and production-acceptance defaults route through the OPL StageRun account/current-owner boundary and return owner receipt, typed blocker, human gate, or route-back refs.
- <a id="live-stage-progress-evidence"></a>`contracts/live_stage_run_progress_evidence.json` records that the retained two-stage pilot evidence is topology-superseded provenance and that current five-stage Live StageRun evidence is deferred. It does not remap historical blockers or receipts into current stages and does not claim live domain progress complete, domain readiness, production readiness, final export acceptance, owner acceptance, or runtime ownership. Immutable OMA topology receipts now live under `docs/evidence/oma-agent-lab/provenance/`, outside active contracts.
- <a id="production-acceptance-tail"></a>`contracts/production_acceptance/bookforge-production-acceptance.json` records the Book Forge production acceptance tail as a domain-owned typed blocker. It exposes the same owner-gated pilot blocker refs through the production-acceptance surface that OPL conformance reads, so the tail is no longer a missing-contract open item. Its default entry boundary points back to the OPL StageRun current-owner route; evidence packages remain output/refs only and cannot explain around owner acceptance. This is a blocker closeout only: owner acceptance, final export acceptance, production readiness, domain readiness, and quality/export readiness remain unclaimed.
- The active stage pack is exactly `storyline-architecture` -> `chapter-production-planning` -> `chapter-materialization` -> `source-style-integrity-review` -> `publication-proof-handoff`. `storyline-architecture` remains the only ordinary default route; `materialize-book` enters `chapter-production-planning` directly. Planning owns storyline admission, readiness, production queue, task-card, and planning-progress refs; `chapter-materialization` owns chapter draft, Markdown, context-pack, QC, repair-log, and review-PDF-eligibility refs. The retired two-stage identifier is allowed only under immutable history/evidence provenance and is machine-guarded from active source, contracts, docs, runtime, scripts, and tests.
- `contracts/action_catalog.json` is now a closed `family-action-catalog.v2` object. Its two public actions carry only the canonical stage-manifest execution binding plus their domain-owned `stage_route`; completion/default-entry/export policy remains in `contracts/temporal_stage_run_consumption_policy.json` instead of being duplicated into the catalog. It contains no hand-written OPL command, derived surface kind, or action handler id/ref. OPL owns the generated `opl agents run --domain opl-bookforge --action <action_id> --workspace <absolute_path>` command and hosted surface projection. The figure-authority callable remains a separate closed-registry `python_callable` reached by `handler:obf.figure-asset-authority-evaluate`.
- All five active prompts now use local Stage goals, good-result criteria, professional dependencies/authority boundaries, and typed closeout shapes. The route preserves storyline -> planning -> materialization -> integrity review -> proof/export dependencies while allowing current-ref reuse, route-back, incremental chapter work, and safe sharded parallelism. The orphan `domain_intake` prompt/policy/capability locator is removed; domain intake maps directly to `storyline-architecture`. This is structural prompt modernization, not new manuscript, runtime, publication, or owner-acceptance evidence.
- `agent/professional_skills/*/SKILL.md` provides five canonical repo-local Codex workflow skills for storyline/style architecture, chapter authoring, source/reference review, meta-review, and publication/memory curation. The current method layer keeps revision-entrypoint routing, long-book chapter-sharded repair, source/reference risk grading, memory delta triage, and publication-proof handoff judgment inside those five skills instead of adding smaller physical skill entries. Legacy fine-grained entries are contract-only redirects in `contracts/capability_map.json#legacy_professional_skill_redirects`; no legacy physical `SKILL.md` or `TOMBSTONE.md` is retained for them. These remain repo-internal and on-demand from the primary skill or stage runtime; they are not the default global Codex entry. This is a structural primary skill / stage prompt / professional skill / tool separation only; it does not prove manuscript quality, runtime execution, memory acceptance, publication readiness, final export readiness, or owner acceptance.
- The PDF export helper uses real typesetting backends, distinguishes `review_pdf`, `publication_proof`, and `final_export`, scans Markdown image refs, checks figure asset manifest readiness, writes rendered-page machine-baseline inspection when requested, reads embedded fonts through `pdffonts` when available, scans rendered-page density and trailing whitespace, and fail-closes `publication_proof` / `final_export` claims when required proof evidence or machine QA fields are missing.
- Book Forge consumes OPL-owned dependency and workspace-lifecycle routes. It does not own OS/TeX package installation, OPL runtime, queue, attempt ledger, generated interface hosting, or app shell routing.
- `contracts/domain_descriptor.json#/standard_agent_interface` is the Book Forge-owned Standard Agent interface source for workspace defaults/locator, runtime registration, progress aliases, and explicit routing signals. OPL consumes those defaults from the package-managed descriptor instead of owning a duplicate Book Forge workspace profile. Direct command templates remain `null` because OPL generated/hosted surfaces and the Codex skill carrier own entry exposure; this repo does not add a private CLI wrapper.
- `contracts/artifact_lifecycle_handoff.json` is the machine-readable Book Forge handoff contract for OPL `workspace artifact-lifecycle` projections. OPL owns the command/readback; Book Forge has no repo-local content-hygiene or lifecycle executable. Domain-specific voice/style rules remain in skills and quality gates, while generic source/lifecycle projection remains in OPL. The contract keeps dry-run/projection-clean states from counting as workspace apply, publication readiness, final export readiness, owner acceptance, or production readiness.
- `contracts/opl_ledger_artifact_registration.json` declares Book Forge's refs-only OPL Ledger artifact registration boundary for long-term book deliverables. OPL Ledger registers artifact refs, hashes, index refs, review refs, and receipt refs; it does not store or mutate manuscript/artifact bodies, sign owner receipts, create typed blockers, write runtime queues/provider attempts, or authorize publication/final-export verdicts. `contracts/generated_surface_handoff.json` exposes the locator/readback projection so generated or ledger-facing callers can discover the contract without creating a second truth source.
- `scripts/verify.sh structural` runs the read-only OPL `workspace source-hygiene` gate once after policy and generated-surface validation; `helpers` uses OPL `pack native-helper probe` for PDF and image-asset handler readback. The image handler is a read-only `python_callable`: OPL generates and materializes the bitmap and injects attempt/output refs, relative path, and SHA-256; Book Forge validates the contained bitmap and returns a figure-authority receipt candidate or quality debt. The default lane does not make a provider call or compile PDFs; this evidence does not prove book delivery, publication readiness, or owner acceptance.
- `contracts/functional_privatization_audit.json` declares all eight OPL-generated or hosted default-surface ids physically absent from this domain repo and applies a no-resurrection boundary instead of retaining repo-local CLI, MCP, Skill, product-entry/session/status, domain-handler, or workbench shells. Fresh Framework `fde7db8f` default-caller readback reports `8/8` retirement gates closed, zero active deletion worklists, zero missing proofs, and a passed source-behavior scan. This structural absence still keeps `physical_delete_authorized=false` and `default_caller_delete_ready=false` and does not synthesize a domain owner receipt or typed blocker.
- `contracts/functional_privatization_audit.json#bookforge_private_platform_retirement_matrix`, `contracts/generated_surface_handoff.json#private_platform_retirement_projection`, `contracts/image_asset_host_handoff.json`, and `contracts/domain_handler_registry.json` classify publication/export, read-only image authority, and runtime/session/update boundaries. The historical project-hygiene helper had no package/default/active caller and has been physically removed. The image handler has no private execution or persistence effect; OPL still owns hosted generation and persistence. This does not authorize owner acceptance, publication readiness, final export readiness, live hosted parity, domain readiness, or production readiness.

## Evidence Packages

Evidence package navigation is owned by [docs/evidence/README.md](./evidence/README.md). Status only records the current claim boundary:

- Tracked evidence supports OMA Agent Lab baseline / takeover work and one historical short-book pilot with owner blockers.
- Package READMEs are manifests and claim-boundary owners; artifact/input/quality Markdown leaves are evidence payloads, not active docs truth owners.
- It does not prove book quality, production readiness, publication readiness, final export readiness, hosted runtime parity, direct runtime parity, physical delete readiness, or owner acceptance.
- Do not add package file lists, verifier transcripts, rendered-page paths, receipt ids, or pilot closeout logs here; new package rows belong in the evidence index, and durable claim changes must fold back to the active plan, core docs, contracts, agent pack files, validators, runtime receipts, owner receipts, or typed blockers.

## Claim Boundary

- Existing evidence proves a valid OPL standard scaffold, generated interface descriptors, standard Foundry Agent public projection/generation ownership contracts, single-default golden path structure, OMA Agent Lab baseline evidence, external-suite self-evolution evidence, and a historical short-book pilot with generated artifacts, exports, rendered pages, quality receipts, and owner blockers.
- Existing evidence does not prove real workspace artifact-lifecycle apply, final production readiness, publication approval, owner acceptance, live OPL StageRun execution, hosted OPL artifact-handoff parity, physical delete authorization, default-caller delete readiness, or book-specific publication-proof visual acceptance.
- Existing evidence also does not prove Temporal/StageRun provider completion as domain completion. The Temporal policy is a structural consumption boundary; domain closeout still requires `owner_receipt_ref`, `typed_blocker_ref`, `human_gate_ref`, or `route_back_ref`, and `final_export` requires owner/export acceptance rather than provider, generated-surface, or StageRun status. The production acceptance tail now has a domain-owned typed-blocker contract, but that contract only records the legal blocker shape and does not authorize any ready claim.
- The historical pilot generator's prose-in-code pattern is superseded by the current Markdown-first, chapter-sharded materialization invariant. Keep the pilot as evidence, not as the current implementation pattern for book-length manuscript source management.

## Next Evidence Required

- Human owner receipt accepting the pilot topic, reader promise, manuscript quality, figure/table plan, DOCX/PDF layout, and publication intent.
- A real book project receipt from `runtime/native_helpers/bookforge_pdf_export.py` using a project-local publication design profile, resource-path-backed asset resolution, rendered-page refs, rendered-page inspection, and owner/export acceptance when the target is `final_export`.
- Live OPL StageRun evidence when making runtime/StageRun claims; the current completion audit is a contract/readback boundary, not live StageRun execution evidence.
- Fresh OPL dependency doctor readback for `bookforge-publication-proof` when making publication-proof or final-export claims.
- A real manuscript run that exercises `revision-entrypoint-router` with independent meta-review evidence, route-back or repair-plan refs, refreshed artifacts, and owner decisions.
- A real Book Forge workspace run that exercises OPL `workspace artifact-lifecycle --apply` against domain-owned `handoff/review-repair-transport.json` and refreshed downstream refs.
- A book-specific publication-proof visual inspection report covering front matter, chapter openings, dense body pages, figure/table pages, callouts, closing pages, and owner-directed design preferences.
- A real publication-proof run that exercises the Kami-inspired Book Forge proof refs: design token receipt, component inventory, font actual-load/readback, rendered-page QA, front matter/TOC cleanliness, page rhythm/density/orphan checks, material/asset coverage, and pre-ship proof review.
- Live OPL StageRun execution and hosted OPL artifact-handoff parity evidence.

The declarative pack now records an explicit risk decision for every Stage: storyline, planning, materialization, and final-byte publication proof use isolated Stage Review; `source-style-integrity-review` is the non-recursive whole-book Meta Review. Primary-only producers are decisive, while formal-Review producer/repairer Attempts only recommend and decisive reviewer/re-reviewer Attempts select the declared route. With repair budget remaining, same-Stage defects continue the local loop; required work whose narrowest canonical owner is a different declared Stage may use the sole early terminal branch, `repair_required + route_back`, to that owner. Publication-proof candidates remain review-pending until fresh exact-byte Review, while downstream owner/export acceptance stays separate. This is structural policy currentness only; it does not supply live review receipts, real route-back generations, publication acceptance, final-export authority, or owner acceptance.
